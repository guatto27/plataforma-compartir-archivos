<%- include('partials/header') %>

<div class="auth-card">
  <h1>Cambiar contraseña</h1>
  <% if (forced) { %>
    <p class="muted">Por seguridad, debes definir una nueva contraseña antes de continuar.</p>
  <% } %>

  <% if (error) { %><div class="flash flash-error"><%= error %></div><% } %>

  <form method="post" action="/cambiar-password" autocomplete="off">
    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
    <label>Contraseña actual
      <input type="password" name="current" required maxlength="200" />
    </label>
    <label>Nueva contraseña (mínimo 10 caracteres)
      <input type="password" name="next" required minlength="10" maxlength="200" />
    </label>
    <label>Confirmar nueva contraseña
      <input type="password" name="confirm" required minlength="10" maxlength="200" />
    </label>
    <button class="btn btn-primary btn-block" type="submit">Guardar</button>
  </form>
</div>

<%- include('partials/footer') %>
